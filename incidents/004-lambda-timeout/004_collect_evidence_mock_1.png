"""
Mock script to simulate collecting evidence for lab 004.
"""

import time

def collect_evidence():
    resources = ["EC2 instance", "S3 bucket", "RDS database"]
    for resource in resources:
        print(f"[EVIDENCE] Collecting logs from {resource}...")
        time.sleep(1)
        print(f"[EVIDENCE] Logs collected for {resource}")

if __name__ == "__main__":
    collect_evidence()
